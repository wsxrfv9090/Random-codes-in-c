#include <stdio.h>
#include "Display.h"

int main()
{
	int tmp1 = Menu();
	switch (tmp1) :
	{
	case 1:
		double arr[1000] = { 0 };
		int i = 0;
		while(i<=1000)
		{
			scanf_s("%lf",arr[i], 1);
			double tmp2 = Add(arr,);
		}

	case 2:
	case 3:
	case 4:
	}
	return 0;
}